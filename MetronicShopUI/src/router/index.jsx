import { BrowserRouter } from 'react-router-dom';
import Route from './router';

export default function Router() {
    return (
        <BrowserRouter>
            <Route></Route>
        </BrowserRouter>
    );
}